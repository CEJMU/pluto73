diff --git a/arch/arm/boot/dts/zynq-pluto-sdr.dtsi b/arch/arm/boot/dts/zynq-pluto-sdr.dtsi
index 1d83aa72ad9e..58e9d88da3fb 100644
--- a/arch/arm/boot/dts/zynq-pluto-sdr.dtsi
+++ b/arch/arm/boot/dts/zynq-pluto-sdr.dtsi
@@ -16,8 +16,21 @@ memory {
 		reg = <0x00000000 0x20000000>;
 	};
 
+	/* Reserved DMA region owned by the Rust app (kept out of Linux's map). */
+	reserved-memory {
+		#address-cells = <1>;
+		#size-cells = <1>;
+		ranges;
+
+		audio_buffer: buffer@1fc00000 {
+			no-map;
+			reg = <0x1fc00000 0x400000>; /* 4 MB */
+		};
+	};
+
 	chosen {
 		stdout-path = "/amba@0/uart@E0001000";
+		bootargs = "uio_pdrv_genirq.of_id=generic-uio";
 	};
 
 	clocks {
@@ -134,6 +147,14 @@ fpga_axi: fpga-axi@0 {
 		#size-cells = <0x1>;
 		ranges;
 
+		/* Audio DMA exposed via UIO so the kernel leaves it to the Rust app. */
+		audio_dma: dma@40400000 {
+			compatible = "generic-uio";
+			reg = <0x40400000 0x10000>;
+			interrupt-parent = <&intc>;
+			interrupts = <0 58 IRQ_TYPE_LEVEL_HIGH>;
+		};
+
 		axi_i2c0: i2c@41600000 {
 			compatible = "xlnx,axi-iic-1.02.a", "xlnx,xps-iic-2.00.a";
 			reg = <0x41600000 0x10000>;
